# gangilyi.xyz 사이트 관리 가이드

이 문서는 사이트를 직접 관리할 때 필요한 모든 것을 담고 있습니다.
(개발자용 요약은 `README.md`, Claude Code용 안내는 `CLAUDE.md` 참고)

---

## 한눈에 보는 구조

```
├── index.html              홈 (내용 없음 — 레이아웃이 전부 그림)
├── about.md                소개 페이지 (/about/) — 짧은 소개글 + CV PDF 링크
├── _projects/              ★ 프로젝트 1개 = 파일 1개
│   ├── _template.md        새 프로젝트용 템플릿 (빌드에서 제외됨)
│   └── cbcb.md, ...        22개 프로젝트
├── img/                    모든 이미지 (프로젝트이름_ 접두어)
├── docs/cv.pdf, cv_en.pdf  CV PDF 한국어/영어 (about 페이지에서 링크됨)
├── _layouts/               페이지 뼈대 (home / project / page / default)
├── _includes/              부품 (nav, footer, 카드, 임베드 플레이어)
├── assets/css/main.css     전체 스타일 (다크 테마)
├── assets/js/home-sketch.js 홈 상단 움직이는 배경 (p5.js)
└── _config.yml             사이트 설정
```

**배포 방식**: `master` 브랜치에 push하면 GitHub Pages가 1~2분 안에 자동으로
빌드해서 https://gangilyi.xyz 에 반영합니다. 별도 배포 명령은 없습니다.

---

## 1. 새 프로젝트 추가하기

1. `_projects/_template.md`를 복사 → `_projects/프로젝트이름.md`로 저장
   - 파일명은 영문 소문자+언더스코어 권장 (예: `night_walk.md`)
   - 파일명이 곧 주소가 됩니다: `/projects/night-walk/` (언더스코어는 하이픈으로 바뀜)
2. front matter를 채웁니다 (아래 항목 설명 참고)
3. 이미지를 `img/`에 넣습니다 (예: `night_walk_main.jpg`, `night_walk_0.jpg`)
4. commit & push → 홈 그리드에 자동으로 나타납니다

홈 구성은 자동입니다:
- **selected works** (상단, 큰 카드) = `featured: true`가 붙은 프로젝트
- **all works** = 전체를 연도 디바이더로 묶어 최신순 정렬, 타입 필터 버튼 제공
- 커버 이미지가 없는 프로젝트는 제목 타이포그래피 타일로 표시됩니다 (이미지 넣고 `cover:` 추가하면 교체됨)

### front matter 항목 설명

```yaml
---
title: "한글 제목"              # 필수. 카드와 페이지 상단에 표시
title_en: "English Title"      # 선택. 한글 제목 아래 작게 표시
year: 2026                     # 권장. 정렬 기준 (없으면 그리드 맨 뒤로 감)
type: performance              # 선택. performance | installation | sound | research | workshop
                               # → 홈의 필터 버튼이 됨. 새 타입을 쓰면 버튼도 자동 생성
cover: /img/파일명.jpg          # 필수. 홈 그리드 썸네일 (본문에 또 넣지 말 것)
featured: true                 # 선택. 홈 상단 "selected works" 대표작 섹션에 노출
media:                         # 선택. 임베드 플레이어 (아래 참고)
  - youtube: "영상ID"
links:                         # 선택. 임베드 안 되는 링크 → 버튼으로 표시
  - label: "code (GitHub)"
    url: "https://github.com/..."
draft_stub: true               # 설명이 아직 없는 미완성 페이지 표시용 (완성하면 삭제)
---
```

front matter 아래 본문은 자유 마크다운입니다 (한/영 혼용 OK):

```markdown
작품 설명 텍스트.

![](/img/night_walk_0.jpg)
![](/img/night_walk_1.jpg)
```

### 주의할 점

- 이미지는 반드시 `![](/img/파일명.jpg)` 형식 (맨 앞 `/` 포함, `<img>` 태그 금지)
- 제목에 `:` 이나 따옴표가 들어가면 반드시 큰따옴표로 감싸기: `title: "제목: 부제"`
- 본문에서 `<작품명>` 처럼 꺾쇠를 쓰면 HTML 태그로 오인됩니다 → `&lt;작품명&gt;` 으로 쓰기

---

## 2. 미디어 임베드 (media:)

| 종류 | 쓰는 법 | ID 찾는 법 |
|---|---|---|
| YouTube | `- youtube: "dQw4w9WgXcQ"` | `youtu.be/` 또는 `watch?v=` 뒤의 문자열 (`?si=`, `&t=` 등 뒤 파라미터는 떼기) |
| Vimeo | `- vimeo: "124137674"` | `vimeo.com/` 뒤의 숫자 |
| SoundCloud | `- soundcloud: "https://soundcloud.com/thisriver/트랙"` | 트랙/플레이리스트 전체 주소 그대로 |
| Bandcamp | `- bandcamp: { album: "숫자ID" }` | Bandcamp 앨범 페이지 → Share/Embed → embed 코드 안의 `album=숫자` 복사 (트랙이면 `{ track: "숫자" }`) |

- 임베드가 안 되는 것들(Google Drive, 재생 시작 시간이 중요한 링크, bit.ly, on.soundcloud.com 단축 링크)은 `media:` 대신 `links:`에 넣으세요.
- 한 페이지에 임베드는 2~3개까지만 (무거워짐). 나머지는 `links:`로.
- Google Drive에 있는 오디오/영상은 임베드 불가 → YouTube(비공개 링크 가능)나 SoundCloud에 올린 뒤 `media:`로 옮기는 걸 권장.

---

## 3. 기존 내용 수정하기

| 고치고 싶은 것 | 파일 |
|---|---|
| 프로젝트 설명/연도/영상 | `_projects/해당파일.md` |
| 약력/CV | 영어 CV는 `_cv/cv_en.html`을 고친 뒤 `./_cv/build.sh` 실행 → `docs/cv_en.pdf` 자동 갱신. 한국어 CV는 `docs/cv.pdf`를 같은 이름으로 덮어쓰기. 짧은 소개글은 `about.md` |
| 홈 하단 소셜 링크 | `_includes/footer.html` |
| 상단 메뉴 | `_includes/nav.html` |
| 색/폰트/간격 | `assets/css/main.css` 맨 위 `:root` 변수 (`--bg` 배경, `--accent` 포인트색 등) |
| 홈 이름/태그라인 | `_layouts/home.html` |
| 움직이는 배경 | `assets/js/home-sketch.js` (파일 안 주석 참고) |
| 사이트 제목/설명(검색 노출) | `_config.yml` 의 `title`, `description` |

텍스트 오타 정도는 GitHub 웹사이트에서 파일을 직접 편집하고 commit해도
똑같이 반영됩니다 (컴퓨터 없이 가능).

### 움직이는 배경 교체하기

`assets/js/home-sketch.js`의 `drawWaves()` 내용을 바꾸면 됩니다.
OpenProcessing 스케치를 가져오려면:
1. 스케치의 `setup()` 내용 → `s.setup` 안으로, `draw()` 내용 → `s.draw` 안으로
2. 모든 p5 함수 앞에 `s.` 붙이기 (`background()` → `s.background()`)
3. `createCanvas`는 기존처럼 `container.offsetWidth, container.offsetHeight` 유지
4. 파일 하단의 정지 장치(탭 숨김/스크롤 아웃/reduce-motion)는 그대로 두기

---

## 4. 로컬에서 미리보기

GitHub Pages는 푸시 후 반영까지 1~3분 걸리므로, 고치는 중에는 내 컴퓨터에서 바로 확인하는 게 편합니다:

```sh
~/Projects/Gang-il-Yi/preview.sh
```

→ 브라우저가 자동으로 http://127.0.0.1:4000 을 엽니다. 파일을 저장하면 자동 새로고침됩니다.
끝낼 때는 터미널에서 `Ctrl+C`. Claude Code 안에서라면 `! ./preview.sh` 로 실행할 수 있습니다.

(스크립트가 하는 일은 아래와 같습니다. 직접 쳐도 됩니다.)

```sh
export PATH="/opt/homebrew/opt/ruby@3.4/bin:$PATH"
cd ~/Projects/Gang-il-Yi
bundle exec jekyll serve --livereload
```

처음 쓰는 컴퓨터라면 먼저 한 번만:

```sh
brew install ruby@3.4
export PATH="/opt/homebrew/opt/ruby@3.4/bin:$PATH"
cd ~/Projects/Gang-il-Yi
bundle install
```

⚠️ `_config.yml`을 고쳤을 때는 serve를 껐다 다시 켜야 반영됩니다.

---

## 5. 문제가 생겼을 때

**push했는데 사이트가 안 바뀜**
- 1~2분 기다린 뒤 강력 새로고침 (⌘+Shift+R)
- GitHub 저장소 → Settings → Pages 에서 빌드 에러 확인
- 빌드 실패의 흔한 원인: front matter YAML 오타 (들여쓰기, 따옴표 안 닫음, `:` 뒤 공백 누락)

**페이지가 이상하게 깨짐**
- 본문에 꺾쇠 `<...>` 를 그대로 썼는지 확인 → `&lt;...&gt;` 로
- 이미지 경로 오타 확인 (대소문자 구분됨: `UK_research.jpg` ≠ `uk_research.jpg`)

**로컬 serve가 안 됨**
- `export PATH=...` 를 빼먹었는지 확인 (시스템 기본 Ruby로는 안 돌아감)
- 그래도 안 되면 `bundle install` 다시 실행

**절대 건드리면 안 되는 것**
- `CNAME` 파일 (지우면 gangilyi.xyz 연결이 끊어짐)
- `Gemfile.lock` (로컬 빌드 버전 고정)

---

## 6. 남은 정리 거리 (2026-07 기준)

- `_projects/vtouch.md` — 포트폴리오 문서에서 설명을 못 찾아 스텁 상태 (`draft_stub: true`). 설명 채우고 그 줄 삭제하면 완성.
- `_projects/ur_series.md` — UR 시리즈 4개 작품을 한 페이지에 묶어둠. 나중에 작품별로 나눠도 됨 (파일 안 주석 참고). "Obsolute Future"는 연도/출처 미확인.
- `_projects/soundcamp2018.md` — 자료상 행사는 2019년이라 `year: 2019`로 설정됨. 파일명/이미지명만 2018.
- majorleague 오디오, n2b 영상 — Google Drive 링크 상태. 재업로드 후 `media:`로 전환 권장.
